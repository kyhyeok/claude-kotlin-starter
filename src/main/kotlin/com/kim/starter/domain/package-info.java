/**
 * 도메인 계층.
 * <p>
 * 외부 의존: JPA 어노테이션만 허용. Spring/Spring Boot/Adapter에 의존하지 않는다.
 * <p>
 * Null 정책: 모든 타입은 기본 non-null. 명시적 nullable은 ?로 표시.
 */
@org.jspecify.annotations.NullMarked
package com.kim.starter.domain;
